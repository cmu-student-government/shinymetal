<p>
  <b><%= pluralize(@user.user_keys.size, 'User Key') %></b><br />
  <ul>
    <% for user_key in @user.user_keys %>
      <li><%= link_to((user_key.name), user_key_path(user_key)) %></li>
    <% end  %>
  </ul>
</p>