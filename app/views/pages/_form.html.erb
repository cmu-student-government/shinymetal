<%= form_for(@page, url: page_path(@page.url)) do |f| %>
  <div class="field">
    <%= f.text_area :message, rows: "30"  %>
  </div>
  <div class="actions">
    <%= f.submit class: "big-button" %>
  </div>
<% end %>

<%= render 'shared/markdown_help' %>
