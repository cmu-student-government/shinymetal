<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title><%= content_for?(:title) ? "#{yield(:title)} - " : nil %>The Bridge API</title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'The Bridge API' %>">
    <meta name="description" content="<%= content_for?(:keywords) ? yield(:keywords) : 'cmu,bridgeapi,thebridge,api,stugov' %>">

    <%= stylesheet_link_tag     'application',      'data-turbolinks-track' => true, media: 'all' %>
    <%= javascript_include_tag  'vendor/modernizr', 'data-turbolinks-track' => true %>

    <%= favicon_link_tag "favicon.ico" %>
    <%= favicon_link_tag "favicon.ico", rel: "apple-touch-icon" %>

    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'benderface.jpg' %>
  </head>

  <body>

    <div id="wrapper">
      <%= render 'layouts/navigation' %>

      <div class="row">
        <main role="main" class="panel s12">
           <%= render 'layouts/messages' %>
           <%= yield %>
        </main>
      </div>

      <%= render 'layouts/footer' %>
    </div>

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </body>
</html>
