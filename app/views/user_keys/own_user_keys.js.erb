<!-- For use with ajax and pagination in own user keys. -->
$('#own_user_keys').html('<%= escape_javascript render partial: 'user_keys/index_partials/index_table', locals: { keys: @user_keys, list_all: false } %>');
$('#paginator').html('<%= escape_javascript paginate(@user_keys, remote: true).to_s %>');