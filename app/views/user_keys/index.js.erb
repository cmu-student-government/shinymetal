<!-- For use with ajax and pagination -->
$('#user_keys').html('<%= escape_javascript render partial: 'user_keys/index_partials/index_table', object: @user_keys  %>');
  $('#paginator').html('<%= escape_javascript paginate(@user_keys, remote: true).to_s %>');