<%= markdown("# #{@page.name}") %>

<%= markdown(@page.message) %>

<% if @current_user and @current_user.role? :admin %>

  <%= link_to 'Edit', edit_page_path(@page.url), class: "small-button" %>

<% end %>