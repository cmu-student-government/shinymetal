<h1>My Keys</h1>

<div id="user_keys">
  <%= render partial: 'user_keys/own_user_keys_partials/own_user_keys_table', object: @user_keys %>
</div>

<!-- Kaminari paginate -->
<div id="paginator">
  <%= paginate @user_keys, remote: true %></div>
</div>

<br>

<%= link_to 'New User key', new_user_key_path, class: "big-button" %>
