  $('#filters').html('<%= escape_javascript render partial: 'filters/index_partials/filter', collection: @filters %>');
  $('#paginator').html('<%= escape_javascript paginate(@users, remote: true).to_s %>');